{
  "name": "@secapi/sdk-js",
  "version": "1.1.0",
  "description": "JavaScript SDK for SEC API filing search, financial statements, ownership data, and filing sections.",
  "private": false,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "zod": "^4.4.3"
  },
  "scripts": {
    "build": "rm -rf dist && bun build src/index.ts --outdir dist --format esm --target node && tsc -p tsconfig.build.json",
    "prepack": "bun run build",
    "prepublishOnly": "bun run build",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "sec",
    "edgar",
    "financial-data",
    "mcp"
  ],
  "homepage": "https://secapi.ai/developers",
  "bugs": {
    "url": "https://github.com/secapi-ai/secapi-js/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/secapi-ai/secapi-js.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^6.0.3"
  }
}
